#!/bin/bash
cd ~/Online-notbook- || exit
git add .
git commit -m "auto update $(date '+%d-%m-%Y %H:%M:%S')" 2>/dev/null
git push
#!/bin/bash
cd ~/Online-notbook- || exit
git add .
git commit -m "auto update $(date '+%d-%m-%Y %H:%M:%S')" 2>/dev/null
git push
